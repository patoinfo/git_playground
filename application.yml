logging:
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'DEBUG'

spring:
  cloud:
    config:
      server:
        git:
         uri: https://github.com/patoinfo/git_playground
         basedir: target/config
security:
   user:
      password: password
